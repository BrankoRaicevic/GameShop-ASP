@model IEnumerable<Product>

@{
    ViewBag.Title = "GameShop";
}


<div class="card-deck">
    @foreach (var product in Model)
    {
    <div class="card m-3" style="min-width: 18rem; max-width:30.5%;">
        <div class="card-header">
            <a asp-controller="home" asp-action="details" asp-route-id="@product.ProductId"><h5>@product.ProductName @if (product.ProductModel != null)@product.ProductModel</h5></a>
        </div>
        @*<img class="card-img-top imageThumbnail" src="@photoPath" asp-append-version="true" />*@
        <div class="card-footer @*text-center*@">
            <h6>@product.Cost €</h6>
        </div>
        @*<div class="card-footer text-center">
            <a asp-controller="home" asp-action="details" asp-route-id="@product.ProductId" class="btn btn-primary m-1">View</a>
            <a asp-action="edit" asp-controller="home" asp-route-id="@product.ProductName" class="btn btn-primary m-1">Edit</a>
            <a href="#" class="btn btn-danger m-1">Delete</a>
        </div>*@
    </div>
    }
</div>